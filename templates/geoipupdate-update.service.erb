# Service that updates MaxMind's 'geoipupdate' client

[Unit]
Description='geoipupdate' client update service
Wants=geoipupdate-update.timer
After=network.target

[Service]
Type=oneshot
ExecStart=<%= @service_update_cmd %>

[Install]
WantedBy=multi-user.target
