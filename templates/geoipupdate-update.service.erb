# Service that updates MaxMind's 'geoipupdate' client

[Unit]
Description='geoipupdate' update service
After=network.target

[Service]
Type=simple
ExecStart=<%= @package_name %><% if @verbose %> -v<% end %> -f <%= @conf_dir %>/GeoIP.conf

[Install]
WantedBy=network-online.target
